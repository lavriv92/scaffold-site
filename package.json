{
  "name": "skeleton-site",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "brunch watch --server",
    "build": "brunch build",
    "deploy": "yarn build && cd public && surge && cd .."
  },
  "dependencies": {
    "brunch": "^2.10.17"
  },
  "devDependencies": {
    "surge": "^0.21.3"
  }
}
